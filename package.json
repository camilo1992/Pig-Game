{
  "name": "starter",
  "version": "1.0.0",
  "description": "There is a dice display and gneration code.\r By generetating a random number and then changing the source image in the element, it is possible to display a dice effect.",
  "scripts": {
    "start": "parcel index.html",
    "build": "parcel build index.html --dist-dir ./dist"
  },
  "author": "Cristian camilo",
  "license": "ISC",
  "dependencies": {
    "parcel": "^2.0.0-rc.0"
  },
  "devDependencies": {
    "@parcel/transformer-image": "^2.0.0-rc.0"
  }
}
